{
  "id": "class-super-call",
  "tier": 7,
  "description": "Triangle's initialize calls super(3) to delegate to Shape's initialize -- the `super'` head, which a rebuilt rule types as whatever the parent's matching method returns -- so the ancestor walk has to run at typing time, not just at dispatch time.",
  "expect_sorbet": true,
  "expect_validate": true,
  "false_reason": null,
  "defs": 3,
  "annotated": true
}
