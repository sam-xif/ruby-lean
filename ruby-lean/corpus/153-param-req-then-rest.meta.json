{
  "id": "param-req-then-rest",
  "tier": 14,
  "description": "def tag(first, *rest) -- required-then-rest, the split `identify.rb`'s `self.repo_url(*urls)` uses. The rule has to bind the prefix positionally and collect the suffix, so the parameter list stops being a zip and becomes a small match.",
  "expect_sorbet": true,
  "expect_validate": true,
  "false_reason": null,
  "defs": 1,
  "annotated": true
}
