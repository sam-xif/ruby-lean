{
  "id": "reopen-integer-is-a-unsafe",
  "tier": 12,
  "description": "**A candidate UNSAFE program.** `isADispatchOk` guards `Judge.isAQuery` by checking the *user* class table for an `is_a?` override \u2014 but only for `.inst` types; at an immediate (`.int`) it answers `true` unconditionally, i.e. it assumes the boot `Integer#is_a?` is intact. Reopening `Integer` and redefining `is_a?` to return a String falsifies that, and `& true` is the operation that turns the wrong type into a real NoMethodError (`bool` has `&`, `String` does not).",
  "expect_sorbet": false,
  "expect_validate": false,
  "false_reason": "unsafe_program",
  "defs": 1,
  "annotated": true,
  "sorbet_note": "Method `&` does not exist on `String`"
}
