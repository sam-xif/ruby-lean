# Harvested from the ruby/ruby source tree (prong 1); regenerate with
# bin/harvest_bootstraptest. Not vendored to keep the repo small and avoid
# committing third-party sources.
corpus/bootstraptest/

# Illustrative desugar dumps (some derived from bootstraptest); regenerate with bin/desugar
samples/

# Ad-hoc scratch from manual bin/desugar experiments (never meant to be committed)
zzz*.rb
/a/
/b/
