{
  "id": "array-heterogeneous",
  "tier": 5,
  "description": "[1, \"a\", true] is runtime-safe real Ruby (Ruby arrays are heterogeneous). Ty.arrayOf requires one element type structurally, but Ty.any already exists for exactly this: arrayOf(Ty.any) is expressible today. No language gap -- the array rule needs to join its element types (falling back to `any`) rather than requiring them equal.",
  "program": {
    "v": 5,
    "ast": [
      "array",
      [
        [
          "int",
          1
        ],
        [
          "str",
          "a"
        ],
        [
          "true"
        ]
      ]
    ]
  },
  "expect_validate": true,
  "false_reason": null
}
