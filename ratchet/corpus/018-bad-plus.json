{
  "id": "bad-plus",
  "tier": 2,
  "description": "1 + true is real, genuinely ill-typed Ruby (TypeError at runtime): the hardcoded Integer#+ rule has no (Int, Bool) case, so validate rejects it -- no dishonest certificate needed, unlike the previous corpus design, because chk synthesizes arithmetic truth structurally rather than checking a claim.",
  "program": {
    "v": 5,
    "ast": [
      "send",
      [
        "int",
        1
      ],
      "+",
      [
        [
          "true"
        ]
      ],
      null
    ]
  },
  "cert": {
    "claims": []
  },
  "expect_validate": false
}
