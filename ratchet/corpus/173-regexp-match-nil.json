{
  "id": "regexp-match-nil",
  "tier": 15,
  "description": "\"abc\".match(/\\d+/).nil? -- the safe half of the previous rung: the `nilable` is consumed by `nil?` rather than by an index. Tier 12's narrowing already handles the shape; what is new is that the nilable comes out of a builtin's *return* type rather than out of `Array#[]`.",
  "program": {
    "v": 5,
    "ast": [
      "send",
      [
        "send",
        [
          "str",
          "abc"
        ],
        "match",
        [
          [
            "regexp_lit",
            "\\d+",
            0
          ]
        ],
        null
      ],
      "nil?",
      [],
      null
    ]
  },
  "expect_validate": true,
  "false_reason": null
}
